import { h, Component } from '../../../deps/preact';
import { File } from './model';

interface Props {
    files: File[];
};

export class IconView extends Component<Props> {
    render() {
        return <p>todo</p>;
    }
};
